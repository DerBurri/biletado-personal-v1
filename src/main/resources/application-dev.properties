
#keycloak.auth-server-url= ${KEYCLOAK_HOST}
#keycloak.realm=${KEYCLOAK_REALM}
#keycloak.resource=${KEYCLOAK_USERNAME}
#keycloak.bearer-only=true
#keycloak.ssl-required=external
#keycloak.securityConstraints[0].authRoles[0]=user
#keycloak.securityConstraints[0].securityCollections[0].name=protected
#keycloak.securityConstraints[0].securityCollections[0].patterns[0]=/api/*

#keycloak.policy-enforcer-config.lazy-load-paths=true
#keycloak.policy-enforcer-config.paths[0].path=/api/admin
#keycloak.policy-enforcer-config.paths[0].claimInformationPointConfig.claims[some-claim]={request.parameter['parameter-a']}
#keycloak.enabled=true


#spring.security.oauth2.resourceserver.jwt.issuer-uri =  http://localhost/auth/realms/biletado
#spring.security.oauth2.resourceserver.jwt.jwk-set-uri = http://localhost/auth/realms/biletado/protocol/openid-connect/certs

keycloak.realm=biletado
keycloak.auth-server-url= http://localhost/auth
keycloak.resource= angular
keycloak.bearer-only=true



spring.datasource.url=jdbc:postgresql://${POSTGRES_PERSONAL_HOST}:${POSTGRES_PERSONAL_PORT}/${POSTGRES_PERSONAL_DBNAME}
spring.datasource.driverClassName=org.postgresql.Driver
spring.datasource.username=${POSTGRES_PERSONAL_USER}
spring.datasource.password=${POSTGRES_PERSONAL_PASSWORD}
spring.sql.init.mode=always
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
#spring.jpa.generate-ddl=true
spring.jpa.hibernate.ddl-auto=none
spring.jpa.hibernate.use-new-id-generator-mappings=true

spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false
spring.sql.init.continue-on-error=true




logging.level.org.hibernate.SQL=info
logging.level.org.hibernate=info
logging.level.javax.persistence=info
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=info
logging.level.org.biletado.logging.RequestLoggingFilter=INFO
#logging.level.org.springframework.web=TRACE
#logging.level.org.zalando.logbook=TRACE

spring.jpa.show-sql=true
# Hibernate ddl auto (create, create-drop, validate, update)
